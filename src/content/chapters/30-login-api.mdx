---
title: "สร้าง Login API"
description: "ตรวจ email และ password แล้วเตรียมคืน token เมื่อ login สำเร็จ"
chapter: 36
icon: shield-check
part: "ภาค 6: Authentication"
---

# สร้าง Login API

Login API มีหน้าที่รับ email และ password ค้นผู้ใช้ ตรวจ password และคืน access token เมื่อข้อมูลถูกต้อง

ในบทนี้เราจะทำ flow login และ error handling ให้ถูกก่อน ส่วนการสร้าง JWT token จริงจะอยู่บทถัดไป

## วิธีเรียนบทนี้

ทำตามลำดับนี้:

1. เพิ่ม exception สำหรับ `401` และ `403`
2. เพิ่ม `LoginAsync` ใน `AuthService`
3. ตรวจ password ด้วย `VerifyHashedPassword`
4. เพิ่ม endpoint `POST /api/v1/auth/login`
5. ทดสอบ login ด้วย temporary token

## สิ่งที่จะใช้ในบทนี้

| สิ่งที่จะใช้ | ความหมาย |
| --- | --- |
| `UnauthorizedException` | ใช้กับกรณี login ไม่ผ่านหรือ token ไม่ถูกต้อง |
| `ForbiddenException` | ใช้กับกรณีรู้แล้วว่า user คือใคร แต่ห้ามใช้งาน |
| `VerifyHashedPassword(...)` | ตรวจ password ดิบกับ password hash |
| `PasswordVerificationResult.Failed` | ผลลัพธ์เมื่อ password ไม่ถูกต้อง |
| temporary token | token ชั่วคราวเพื่อให้ flow login compile ก่อนสร้าง JWT จริง |

## หลังจบบทนี้ ไฟล์ที่เปลี่ยน

```text
Exceptions/UnauthorizedException.cs
Exceptions/ForbiddenException.cs
Services/AuthService.cs
Controllers/AuthController.cs
```

## ขั้นที่ 1: สร้าง exception เพิ่ม

รันจากโฟลเดอร์ `Backend.Api`

Windows PowerShell:

```powershell
New-Item -ItemType File -Path Exceptions/UnauthorizedException.cs
New-Item -ItemType File -Path Exceptions/ForbiddenException.cs
```

macOS/Linux Bash:

```bash
touch Exceptions/UnauthorizedException.cs
touch Exceptions/ForbiddenException.cs
```

## ขั้นที่ 2: เพิ่ม UnauthorizedException

เปิดไฟล์:

```text
Exceptions/UnauthorizedException.cs
```

เพิ่ม code นี้:

```csharp
using Microsoft.AspNetCore.Http;

namespace Backend.Api.Exceptions;

public class UnauthorizedException(string message, string code)
    : ApiException(message, code, StatusCodes.Status401Unauthorized);
```

ใช้เมื่อ login ไม่ผ่าน ไม่ว่าจะเพราะ email ไม่มีอยู่หรือ password ผิด

## ขั้นที่ 3: เพิ่ม ForbiddenException

เปิดไฟล์:

```text
Exceptions/ForbiddenException.cs
```

เพิ่ม code นี้:

```csharp
using Microsoft.AspNetCore.Http;

namespace Backend.Api.Exceptions;

public class ForbiddenException(string message, string code)
    : ApiException(message, code, StatusCodes.Status403Forbidden);
```

ใช้เมื่อรู้แล้วว่าผู้ใช้คือใคร แต่บัญชีถูกปิดใช้งานหรือไม่มีสิทธิ์

## ขั้นที่ 4: เริ่ม LoginAsync

เปิดไฟล์:

```text
Services/AuthService.cs
```

เพิ่ม method นี้ใน class `AuthService`

```csharp
public async Task<LoginResponse> LoginAsync(LoginRequest request)
{
    var user = await userRepository.GetByEmailAsync(request.Email);

    if (user is null)
    {
        throw new UnauthorizedException(
            "Invalid email or password",
            "INVALID_CREDENTIALS");
    }
```

ถ้า email ไม่พบ ให้ตอบข้อความกลาง ๆ ว่า `Invalid email or password`

## ขั้นที่ 5: ตรวจ password

เพิ่ม code นี้ต่อใน method `LoginAsync`

```csharp
    var verificationResult = passwordHasher.VerifyHashedPassword(
        user,
        user.PasswordHash,
        request.Password);

    if (verificationResult == PasswordVerificationResult.Failed)
    {
        throw new UnauthorizedException(
            "Invalid email or password",
            "INVALID_CREDENTIALS");
    }
```

`VerifyHashedPassword` คือวิธีที่ถูกต้องในการตรวจ password เพราะ password hash มี salt และไม่ควรถูกเทียบด้วย string ธรรมดา

## ขั้นที่ 6: ตรวจบัญชี inactive

เพิ่ม code นี้ต่อจากการตรวจ password:

```csharp
    if (!user.IsActive)
    {
        throw new ForbiddenException(
            "User account is inactive",
            "USER_INACTIVE");
    }
```

กรณีนี้ใช้ `403 Forbidden` เพราะ user มีอยู่และ password ถูกต้องแล้ว แต่ระบบไม่อนุญาตให้ใช้งานบัญชีนี้

## ขั้นที่ 7: คืน temporary token

ปิดท้าย method ด้วย response ชั่วคราว:

```csharp
    return new LoginResponse
    {
        AccessToken = "temporary-token-created-in-next-chapter",
        TokenType = "Bearer",
        ExpiresIn = 0
    };
}
```

ตอนนี้ method นี้ยังคืน temporary token เพื่อให้โปรเจกต์ compile และทดสอบ flow login ได้ก่อน ในบทถัดไปเราจะแทนส่วนนี้ด้วย JWT token จริง

## ทำไม email หรือ password ผิดต้องตอบข้อความเดียวกัน

ถ้า email ไม่พบแล้วตอบว่า `Email not found` แต่ password ผิดตอบว่า `Password is incorrect` ผู้โจมตีจะใช้ API ตรวจได้ว่า email ไหนมีอยู่ในระบบ

ดังนั้นทั้งสองกรณีควรตอบข้อความกลาง ๆ ว่า `Invalid email or password`

## ขั้นที่ 8: เพิ่ม endpoint login

เปิดไฟล์:

```text
Controllers/AuthController.cs
```

เพิ่ม action นี้ใน `AuthController`

```csharp
[HttpPost("login")]
public async Task<IActionResult> Login(LoginRequest request)
{
    var response = await authService.LoginAsync(request);

    return Ok(response);
}
```

`LoginRequest` ถูก validate ด้วย Data Annotations ก่อนเข้า action เพราะ controller มี `[ApiController]`

## ตรวจ build

รันจากโฟลเดอร์ `Backend.Api`

```powershell
dotnet build
```

ถ้า build error ว่าไม่รู้จัก `PasswordVerificationResult` ให้ตรวจว่า `AuthService.cs` มี using นี้:

```csharp
using Microsoft.AspNetCore.Identity;
```

## ทดสอบ login flow เบื้องต้น

หลังจบบทนี้ login ที่ email/password ถูกต้องจะได้ temporary token กลับมา

```http
@baseUrl = http://localhost:<http-port>
@authPath = /api/v1/auth

### Login
POST {{baseUrl}}{{authPath}}/login
Content-Type: application/json

{
  "email": "demo-user@example.com",
  "password": "User1234!"
}
```

ผลลัพธ์ที่คาดหวังชั่วคราวคือ:

```json
{
  "accessToken": "temporary-token-created-in-next-chapter",
  "tokenType": "Bearer",
  "expiresIn": 0
}
```

บทถัดไปจะเปลี่ยน response นี้ให้เป็น JWT จริง

## ทดสอบกรณี login ไม่ผ่าน

ลอง password ผิด:

```http
### Login with wrong password
POST {{baseUrl}}{{authPath}}/login
Content-Type: application/json

{
  "email": "demo-user@example.com",
  "password": "wrong-password"
}
```

ควรได้ `401 Unauthorized` พร้อม `code` เป็น `INVALID_CREDENTIALS`

ลอง email ที่ไม่มีในระบบ:

```http
### Login with missing email
POST {{baseUrl}}{{authPath}}/login
Content-Type: application/json

{
  "email": "missing@example.com",
  "password": "User1234!"
}
```

ควรได้ `401 Unauthorized` และยังใช้ข้อความกลาง ๆ เหมือน password ผิด เพื่อไม่บอกผู้โจมตีว่า email ไหนมีอยู่จริง

ลองบัญชี inactive จาก seed data:

```http
### Login inactive user
POST {{baseUrl}}{{authPath}}/login
Content-Type: application/json

{
  "email": "inactive-user@example.com",
  "password": "User1234!"
}
```

ควรได้ `403 Forbidden` พร้อม `code` เป็น `USER_INACTIVE`

ตัวอย่าง error body สำคัญควรมี `code` เพื่อให้ frontend แยกกรณีได้:

```json
{
  "title": "Invalid email or password",
  "status": 401,
  "code": "INVALID_CREDENTIALS"
}
```

บทนี้ยังไม่ทำ account lockout หรือ rate limiting เพราะต้องเพิ่ม state และ policy อีกหลายส่วน เรื่องนั้นจะกลับมาในภาค production hardening

## Checkpoint

ก่อนอ่านบทต่อไป ให้ตรวจว่าทำได้ครบตามนี้

- มี `UnauthorizedException`
- มี `ForbiddenException`
- `AuthService.LoginAsync` ค้น user จาก email
- ตรวจ password ด้วย `VerifyHashedPassword`
- email หรือ password ผิดตอบ `INVALID_CREDENTIALS`
- บัญชี inactive ตอบ `USER_INACTIVE`
- มี `POST /api/v1/auth/login` ใน `AuthController`
